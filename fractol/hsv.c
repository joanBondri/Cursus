float	mod(float x, float m)
{
	int		yop;
	float	

	yop = (int)x;

}

int		hvs_to_hex(float h, float s, float v)
{
	float	ix;
	float	em;
	float	ce;

	ce = v * s;
	ix = ce * (1 * mod(abs((h / 60.0), 2) - 1));
	if (h < 60)
		return ()
}
